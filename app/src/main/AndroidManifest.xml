<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dreamwalker.diabeteseducation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.Intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.EduImageActivity" />
        <activity android:name=".activity.EduPdfActivity" />
        <activity android:name=".activity.EduWordActivity" />
        <activity android:name=".activity.MainActivity"/>
        <activity android:name=".activity.AppIntroductionActivity"/>
        <activity android:name=".introduction.ChangeLogActivity"/>
        <activity android:name=".introduction.DevelopActivity"/>
        <activity android:name=".introduction.IntroActivity"/>
    </application>

</manifest>